namespace Yui.Entities.Commands
{
    
    /*
     * actually, those are useless
     * i can just use normal strings
     */
    public enum Order
    {
        First,
        Random,
        Last
    }
}